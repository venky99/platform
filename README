That's Galaxy GIO manifest for CM7:

For compiling, in terminal:
$ mkdir ~/bin
$ PATH=~/bin:$PATH
$ curl https://dl-ssl.google.com/dl/googlesource/git-repo/repo > ~/bin/repo
$ chmod a+x ~/bin/repo
$ mkdir CM7
$ cd CM7
$ repo init -u https://github.com/MoltenMotherBoard/manifest -b cm7
$ repo sync
$ . build/envsetup.sh
$ lunch /* Choose your build */

Now in terminal:
$ make bacon

In essence, is the same method used for any Android distribution.
