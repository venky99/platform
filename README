That's Galaxy GIO manifest for AOSP ICS:

For compiling, in terminal:
$ mkdir ~/bin
$ PATH=~/bin:$PATH
$ curl https://dl-ssl.google.com/dl/googlesource/git-repo/repo > ~/bin/repo
$ chmod a+x ~/bin/repo
$ mkdir ICS
$ cd ICS
$ repo init -u https://github.com/MoltenMotherBoard/ics-manifest
$ repo sync
$ . build/envsetup.sh
$ lunch /* Choose your build */

Now replace in framework-base those folders with CyanogenMod ones:
  ICS/frameworks/base/libs/hwui;
  ICS/frameworks/base/opengl;

Now in terminal:
$ make

In essence, is the same method used for any Android distribution.
